import { en } from "./resources/en";
import { it } from "./resources/it";

export { en, it };

/** Plain message catalogs ready to merge into a React Admin i18n provider. */
export const complianceWidgetMessages = { en, it };
