export enum DataItemTypes {
    DATAITEM = 'dataitem',
}

export const getDataItemUiSpec = (kind: string | undefined) => {
    if (!kind) {
        return undefined;
    }

    return undefined;
};


