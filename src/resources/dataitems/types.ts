export enum DataItemTypes {
    DATAITEM = 'dataitem',
}

export const getDataItemSpecUiSchema = (kind: string | undefined) => {
    if (!kind) {
        return undefined;
    }

    return undefined;
};


