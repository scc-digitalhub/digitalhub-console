export enum ArtifactTypes {
    ARTIFACT = 'artifact',
}

export const getArtifactSpecUiSchema = (kind: string | undefined) => {
    if (!kind) {
        return undefined;
    }
    return undefined;
};
